#include <iostream>

using namespace std;

int main() {
    float a,b,c,d,e,f,sum;
    cin >> a >> b >> c >> d >> e >> f;
    sum=a*13+b*7+c*5+d*3+e*3+f*2;
    cin >> a >> b >> c >> d >> e >> f;
    cout << (sum>a*13+b*7+c*5+d*3+e*3+f*2+1.5?"cocjr0208":"ekwoo");
}